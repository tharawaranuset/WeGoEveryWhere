export interface SwaggerConfig {
  swagger: {
    title: string;
    description: string;
    version: string;
  };

  'swagger.title'?: string;
  'swagger.description'?: string;
  'swagger.version'?: string;
}